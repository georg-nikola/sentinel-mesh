# Enable legacy peer deps for Vite 7 + @vitejs/plugin-vue@5 compatibility
# Vite 7 is newer than what @vitejs/plugin-vue@5 officially supports (v5-6)
# but works correctly with this flag enabled
legacy-peer-deps=true
